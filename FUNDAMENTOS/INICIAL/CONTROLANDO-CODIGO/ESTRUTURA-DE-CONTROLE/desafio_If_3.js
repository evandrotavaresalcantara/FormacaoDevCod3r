const x = 10;
const y = 8;

const operacao = "*";
let resultado;

if (operacao === "+")
    resultado = x + y;

if (operacao === "-")
    resultado = x - y;

if (operacao === "*")
    resultado = x * y;

if (operacao === "/")
    resultado = x / y;

console.log(resultado)
